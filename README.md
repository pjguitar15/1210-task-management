# Server (Laravel API) — Setup Guide

This project uses **Laravel + MySQL** via **Laravel Sail (Docker)**.

---

## Requirements

- Docker Desktop (or Docker Engine on Linux)
- Git
- No local PHP or MySQL installation needed

---

## 1) Navigate to the server folder

From the repository root:

```bash
cd server
